import 'package:flutter/material.dart';

class SupportListPage extends StatelessWidget {
  const SupportListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("support list"),
    );
  } 
}